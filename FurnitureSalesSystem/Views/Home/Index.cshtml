@{
    ViewData["Title"] = "Trang chủ - AmLive";
}
<head>
    <style>
        :root {
            --primary-color: #6D4C41;
            --secondary-color: #8D6E63;
            --accent-color: #A1887F;
            --light-color: #F5F5F5;
            --dark-color: #3E2723;
        }

        body {
            font-family: 'Quicksand', sans-serif;
            background-color: var(--light-color);
        }

        .btn-primary {
            background-color: var(--primary-color);
            color: white;
            transition: background-color 0.3s ease, transform 0.2s ease;
        }

            .btn-primary:hover {
                background-color: var(--dark-color);
                transform: translateY(-2px);
            }

        .btn-outline {
            border: 2px solid var(--primary-color);
            color: var(--primary-color);
            transition: all 0.3s ease;
        }

            .btn-outline:hover {
                background-color: var(--primary-color);
                color: white;
            }
    </style>
</head>

<body>
    <div class="min-h-screen bg-[var(--light-color)] flex flex-col">
        <!-- Header -->
        <header class="bg-white shadow-md">
            <div class="max-w-7xl mx-auto px-6 py-4 flex justify-between items-center">
                <div class="flex items-center space-x-2">
                    <i class="fas fa-couch text-2xl text-[var(--primary-color)]"></i>
                    <span class="text-xl font-bold text-[var(--dark-color)]">AmLive</span>
                </div>
            </div>
        </header>

        <!-- Main -->
        <main class="flex-1 relative">
            <!-- Background Image -->
            <img src="https://images.unsplash.com/photo-1615874959474-d609969a2a1c?q=80&w=2070&auto=format&fit=crop"
                 class="absolute inset-0 w-full h-full object-cover opacity-20" />

            <!-- Overlay -->
            <div class="absolute inset-0 bg-gradient-to-r from-white/80 via-white/60 to-white/80"></div>

            <!-- Content -->
            <div class="relative z-10 max-w-4xl mx-auto px-6 py-20 text-center">
                <h1 class="text-4xl md:text-5xl font-bold text-[var(--dark-color)] mb-6">Chào mừng bạn đến với AmLive</h1>
                <p class="text-gray-700 text-lg md:text-xl mb-10">
                    Khám phá các sản phẩm nội thất cao cấp và quản lý đơn hàng của bạn một cách dễ dàng.
                </p>
                </div>
            </div>
        </main>

        <!-- Footer -->
        <footer class="bg-white mt-auto shadow-inner">
            <div class="max-w-6xl mx-auto px-6 py-4 text-center text-sm text-gray-500">
                © 2025 AmLive. All rights reserved.
            </div>
        </footer>
    </div>
</body>
</html>
